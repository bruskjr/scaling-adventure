# language setting
language: ruby

# version numbers, testing against two versions of node
rvm:
  - 2.2.3 # (current default)
env:
  global:
    - CI_REPORTS=spec/reports COVERAGE_REPORTS=coverage/rcov